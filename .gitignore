# ============================================================
# Trading Intelligence Application — Git Ignore Configuration
# ============================================================
# Monorepo: Turborepo + pnpm workspaces
# Apps: apps/api (Express), apps/web (React + Vite)
# Packages: packages/types, packages/config, packages/utils
# ============================================================

# ----------------------------------------------------------
# Dependencies
# ----------------------------------------------------------
node_modules/
.pnpm-store/

# ----------------------------------------------------------
# Build output
# ----------------------------------------------------------
dist/
.turbo/
coverage/
*.tsbuildinfo

# ----------------------------------------------------------
# Environment and secrets
# ----------------------------------------------------------
# CRITICAL: Never commit API keys, tokens, or credentials.
# Use .env.example (committed) as the template.
.env
.env.local
.env.*.local
*.local

# ----------------------------------------------------------
# IDE and editor files
# ----------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ----------------------------------------------------------
# Log files
# ----------------------------------------------------------
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ----------------------------------------------------------
# Temporary files
# ----------------------------------------------------------
tmp/

# ----------------------------------------------------------
# Vercel
# ----------------------------------------------------------
.vercel

# ----------------------------------------------------------
# NOTE: The following are intentionally NOT ignored:
#   - .env.example        (template for required env vars)
#   - apps/api/src/db/migrations/  (SQL migrations are tracked)
#   - pnpm-lock.yaml      (lockfile ensures reproducible installs)
# ----------------------------------------------------------
