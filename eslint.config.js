/**
 * ESLint v9 Flat Config Entry Point — Trading Intelligence Monorepo
 *
 * ESLint v9.x auto-discovers this `eslint.config.js` file at the project root.
 * The canonical configuration lives in `.eslintrc.js` (as specified by the AAP).
 * This file re-exports that config so ESLint's automatic discovery mechanism
 * picks it up without requiring a `--config` flag.
 *
 * @see .eslintrc.js — Canonical root ESLint configuration
 */

export { default } from './.eslintrc.js';
